apiVersion: v1
kind: Service
metadata:
  name: containerize-frontend
  labels:
    app: containerize-frontend
spec:
  type: NodePort
  selector:
    app: containerize-frontend
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30400
      protocol: TCP
